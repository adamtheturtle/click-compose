Installation
============

|project| supports Python |minimum-python-version|\ +.

.. code-block:: shell

   $ pip install click-compose

Or with ``uv``:

.. code-block:: shell

   $ uv add click-compose
