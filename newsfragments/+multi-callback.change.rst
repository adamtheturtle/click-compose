Replace ``compose_callbacks`` with ``multi_callback`` as the single composition helper.
Tuple overloads type-check up to ten type-changing callbacks, and typed lists support any number of same-type callbacks.
